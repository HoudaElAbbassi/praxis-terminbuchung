# Database
# Lokal: postgresql://user:password@localhost:5432/praxis_terminbuchung
# Produktion: Verwenden Sie einen Cloud-Anbieter wie Neon (https://neon.tech), Supabase, oder Vercel Postgres
DATABASE_URL="postgresql://user:password@localhost:5432/praxis_terminbuchung"

# NextAuth / Auth.js
# Lokal: http://localhost:3000
# Produktion: https://ihre-domain.netlify.app
NEXTAUTH_URL="http://localhost:3000"
# WICHTIG: Generieren Sie mit: openssl rand -base64 32
# NextAuth v5 akzeptiert NEXTAUTH_SECRET oder AUTH_SECRET
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"
# Alternative (für NextAuth v5):
# AUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Email Configuration (Resend - empfohlen)
# Resend API Key - Kostenlos: 3.000 E-Mails/Monat
# Erstellen Sie einen Account auf https://resend.com
# Generieren Sie einen API Key unter: Settings → API Keys
RESEND_API_KEY=""

# Absender E-Mail-Adresse
# Kann Ihre eigene Domain sein (z.B. noreply@ihre-praxis.de)
# Domain muss in Resend verifiziert werden für Production
EMAIL_FROM="noreply@praxis-example.de"

# Praxis E-Mail-Adresse (empfängt Benachrichtigungen über neue Terminanfragen)
PRACTICE_EMAIL="info@ihre-praxis.de"

# Alternative: SMTP (nicht empfohlen - weniger sicher)
# EMAIL_SERVER="smtp://user:password@smtp.example.com:587"

# App Settings
APPOINTMENT_SLOT_DURATION=15
BUFFER_TIME=5
OPENING_TIME=8
CLOSING_TIME=16
