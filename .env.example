# Database
# Lokal: postgresql://user:password@localhost:5432/praxis_terminbuchung
# Produktion: Verwenden Sie einen Cloud-Anbieter wie Neon (https://neon.tech), Supabase, oder Vercel Postgres
DATABASE_URL="postgresql://user:password@localhost:5432/praxis_terminbuchung"

# NextAuth / Auth.js
# Lokal: http://localhost:3000
# Produktion: https://ihre-domain.netlify.app
NEXTAUTH_URL="http://localhost:3000"
# WICHTIG: Generieren Sie mit: openssl rand -base64 32
# NextAuth v5 akzeptiert NEXTAUTH_SECRET oder AUTH_SECRET
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"
# Alternative (für NextAuth v5):
# AUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Email (SMTP via Strato oder anderer Provider)
# Beispiel für Strato SMTP-Einstellungen:
# SMTP_HOST: smtp.strato.de
# SMTP_PORT: 587 (STARTTLS) oder 465 (SSL)
SMTP_HOST="smtp.your-provider.com"
SMTP_PORT="587"
SMTP_SECURE="false"  # true für Port 465 (SSL), false für Port 587 (STARTTLS)
SMTP_USER="your-email@your-domain.com"  # Ihre vollständige E-Mail-Adresse
SMTP_PASSWORD="your-email-password"  # Ihr E-Mail-Passwort

# Absender-Email (muss mit SMTP_USER übereinstimmen)
EMAIL_FROM="your-email@your-domain.com"

# Praxis-Email für interne Benachrichtigungen (wohin Terminanfragen gesendet werden)
PRACTICE_EMAIL="practice-email@your-domain.com"

# App Settings
APPOINTMENT_SLOT_DURATION=15
BUFFER_TIME=5
OPENING_TIME=8
CLOSING_TIME=16

# Vorschlags-System-Konfiguration
# Anzahl der Tage, nach denen eine Erinnerung an den Admin gesendet wird
PROPOSAL_REMINDER_DAYS=3

# Admin-Email für Benachrichtigungen (optional, fällt zurück auf PRACTICE_EMAIL oder EMAIL_FROM)
ADMIN_EMAIL="admin@your-domain.com"

# Base URL für E-Mail-Links (Lokal: http://localhost:3000 | Produktion: https://ihre-domain.de)
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
